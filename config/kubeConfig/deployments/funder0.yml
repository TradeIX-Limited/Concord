apiVersion: "apps/v1beta1"
kind: Deployment
metadata:
  name: funder0
spec:
  replicas: 1
  selector:
    matchLabels:
      app: funder0
  template:
    metadata:
      labels:
        app: funder0
    spec:
      containers:
      - name: node
        image: tradeixdev.azurecr.io/funder:1.0
        imagePullPolicy: Always
        args: ["notary"]
#        env:
#        - name: "CONFIG_FILE"
#          value: "node.conf"
        ports:
        - containerPort: 10002
        volumeMounts:
        - name: pvclaim
          mountPath: /opt/corda/node.conf
          subPath: node.conf
          readOnly: true
        - name: additional-node-infos
          mountPath: "/opt/corda/additional-node-infos/"
          readOnly: true
        - name: funder0-certificates
          mountPath: "/opt/corda/certificates/"
          readOnly: true
        - name: funder0-nodeinfo
          mountPath: /opt/corda/nodeInfo
          subPath: nodeInfo
        - name: pvclaim
          mountPath: /opt/corda/logs
          subPath: logs
        - name: pvclaim
          mountPath: /opt/corda/network-parameters
          subPath: network-parameters
        - name: pvclaim
          mountPath: /opt/corda/persistence.mv.db
          subPath: persistence.mv.db
      - name: receiver
        image: tradeixdev.azurecr.io/funder_receiver:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 10004
        volumeMounts:
        - name: receiverconfig
          mountPath: /opt/corda/bin/application.properties
          subPath: application.properties
          readOnly: true
      - name: responder
        image: tradeixdev.azurecr.io/funder_responder:1.0
        imagePullPolicy: Always
        volumeMounts:
        - name: responderconfig
          mountPath: /opt/corda/bin/application.properties
          subPath: application.properties
          readOnly: true
      imagePullSecrets:
      - name: acr-secret
      volumes:
      - name: funder0-nodeinfo
        secret:
          secretName: funder0-nodeinfo
      - name: additional-node-infos
        secret:
          secretName: additional-node-infos
      - name: funder0-certificates
        secret:
          secretName: funder0-certificates
      - name: receiverconfig
        configMap:
          name: funder0-receiver
      - name: responderconfig
        configMap:
          name: funder0-responder
      - name: logs
        emptyDir: {}
      - name: pvclaim
        persistentVolumeClaim:
          claimName: pvclaim-funder0
          readOnly: false
